{
  "build": {
    "builder": "nixpacks",
    "config": {
      "buildCommand": "mkdir -p storage/logs bootstrap/cache && touch storage/logs/laravel.log && chmod -R 777 storage bootstrap/cache",
      "startCommand": "php artisan config:clear && php artisan serve --host=0.0.0.0 --port=8080 --public=public"
    }
  }
}
