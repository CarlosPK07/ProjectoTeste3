{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "mkdir -p storage/logs && touch storage/logs/laravel.log && chmod -R 775 storage bootstrap/cache && php artisan serve --host=0.0.0.0 --port=8080 --public=public"
    }
  }
}
