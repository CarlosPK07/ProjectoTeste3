{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "php artisan serve --host=0.0.0.0 --port=8080 --public=public"
    }
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=8080 --public=public"
  }
}
