{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "php artisan serve --host=0.0.0.0 --port=3000 --public=public"
    }
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=3000 --public=public"
  }
}
